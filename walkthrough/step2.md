# Pull the model

Open your terminal and run:

```bash
ollama pull qwen2.5:3b
```

This downloads the default model (~2GB).

Ollama usually runs automatically in the background. You don’t need to manually start it.

If it's not running, start it with:

```bash
ollama serve
```
